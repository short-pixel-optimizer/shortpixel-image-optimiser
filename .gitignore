vendor
# Test-only dependencies (composer.tests.json) — composer.tests.lock IS tracked.
vendor-tests
composer.lock
.DS_Store
.env.testing
.phpunit.result.cache
.idea/.gitignore
.idea/misc.xml
.idea/modules.xml
.idea/php.xml
.idea/spio2.iml
.idea/vcs.xml
.idea/phpunit.xml
/.idea/deployment.xml
/.idea/shortpixel-image-optimiser.iml
/.idea/workspace.xml
/.idea
/composer.phar
.env.local
/wordpress/
/travis_output/
# Commercial partner-plugin zips for bin/test.sh --compat (WPML etc.) —
# never commit these; drop the zip in and the harness picks it up.
/tests/partner-plugins/
# Manual tests, not needed in the repo
/tests/manual/
