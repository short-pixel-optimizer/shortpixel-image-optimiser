# Mounted over /etc/apache2/ports.conf in the E2E `wordpress` container.
# 8030 is added so the site answers on the same port inside the container
# as it is published on the host — see the URL-scheme note in
# docker-compose.e2e.yml.
Listen 80
Listen 8030
