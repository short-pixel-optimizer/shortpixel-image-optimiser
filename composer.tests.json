{
    "name": "short-pixel-optimizer/spio-tests",
    "description": "Test-only and lint-only dependencies for SPIO. Kept separate from composer.json, which is the plugin/module build tool. Install with: COMPOSER=composer.tests.json composer install (writes composer.tests.lock, installs into vendor-tests/). bin/test.sh and the CI workflows do this automatically. Binaries land in vendor-tests/bin/ — phpunit and phpcs.",
    "require-dev": {
        "phpunit/phpunit": "^9.6",
        "yoast/phpunit-polyfills": "^1.1 || ^2.0",
        "squizlabs/php_codesniffer": "^3.7.2",
        "wp-coding-standards/wpcs": "^3.0",
        "dealerdirect/phpcodesniffer-composer-installer": "^1.0"
    },
    "config": {
        "vendor-dir": "vendor-tests",
        "platform": {
            "php": "7.4.33"
        },
        "allow-plugins": {
            "dealerdirect/phpcodesniffer-composer-installer": true
        }
    }
}
